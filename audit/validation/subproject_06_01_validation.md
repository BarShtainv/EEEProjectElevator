# SP-06.1 Validation

## Environment and commands

Commands were run from `/mnt/c/Users/Bar/Desktop/EEEProjectElevator` at starting commit `7a0b173724038aeac9f25f783a52a586a83619bf` on branch `main`.

```text
$ python --version
Python 3.13.13

$ python -m pytest --version
/home/bar/miniforge3/bin/python: No module named pytest

$ PYTHONPATH=src python -m pytest tests/unit/test_models.py tests/unit/test_config.py tests/unit/test_clock.py
/home/bar/miniforge3/bin/python: No module named pytest
```

Both pytest commands exited unsuccessfully before collection. Exact pytest summary: none—pytest is unavailable. `python3` resolves to the same Python 3.13.13 environment; `/usr/bin/python3` is Python 3.14.4 and also lacks pytest. No pytest executable is on PATH. No install or network action was attempted.

## Compile and import

```text
$ python -m compileall -q src
<no output; exit 0>

$ python -m compileall -q tests
<no output; exit 0>

$ PYTHONPATH=src python -c "import elevator_access_sim"
<no output; exit 0>
```

Generated bytecode/cache directories were removed after validation; they are not repository artifacts.

## Package metadata

Python `tomllib` parsed `pyproject.toml` and confirmed:

```text
BUILD_BACKEND=setuptools.build_meta
PROJECT_NAME=elevator-access-sim
VERSION=0.1.0
REQUIRES_PYTHON=>=3.11
RUNTIME_DEPENDENCIES=0
OPTIONAL_TEST_DEPENDENCY=pytest>=7
PACKAGE_DISCOVERY=src
PYTEST_TESTPATHS=tests
```

No lockfile, virtual environment, coverage/lint/format/type/docs tool, or distributable build was created.

## Independent model checks

A standard-library inline script imported only the implemented package and checked:

```text
ReaderSource values=1,2 text=LF,HF
ControllerState values=0..6 text=lowercase
EventType values=1..6 text=lowercase
Result values=1..5 text=lowercase
Reason values=1..17 text=lowercase
null-value zero absent from source/event/result/reason=yes
frozen dataclass mutation rejected=yes
CredentialKey ordered-pair inequality=yes
raw CredentialRequest malformed values accepted=yes
OutputSnapshot valid zero/one active states=yes
OutputSnapshot length/type/population/floor/expiry invalid states rejected=yes
Boolean-as-integer traps rejected=yes
MODEL_CHECKS=passed
```

An AST/dataclass inspection also confirms all 15 required shared records use `frozen=True, slots=True`, and the exception inheritance matches the reviewed hierarchy.

## Independent configuration checks

The inline validation exercised the documented object, defaults, output endpoints 100/30000, watchdog endpoints 1/4294967295, duplicate member, unknown field, malformed/non-object input, invalid ranges, Boolean integer traps, and NaN. Results:

```text
EXACT_FIELD_SET=5
DOCUMENTED_CONFIG=accepted
DEFAULT_CONFIG=matches
ENDPOINTS=accepted
DUPLICATE_KEY=rejected
UNKNOWN_FIELD=rejected
MALFORMED_AND_NON_OBJECT=rejected
BOOLEAN_AS_INTEGER=rejected
OUT_OF_RANGE=rejected
NONFINITE=rejected
FAILED_PARSE_CHANGES_STATE=no
CONFIG_CHECKS=passed
```

Source/API inspection confirms `load_credentials_json` and `load_startup_json` are absent as required.

## Independent clock checks

The inline validation covered start zero, zero/positive advance, equal/later target, negative/Boolean delta, backward/Boolean target, and state after failure:

```text
NONNEGATIVE_EXACT_INTEGER=yes
ZERO_AND_EQUAL_ADVANCE=yes
FORWARD_MONOTONICITY=yes
NEGATIVE_BACKWARD_BOOLEAN_REJECTION=yes
STATE_UNCHANGED_AFTER_REJECTION=yes
WALL_CLOCK_SLEEP_THREAD_ASYNC_IMPORTS=none
CALLBACK_OR_SCHEDULER_BEHAVIOR=none
CLOCK_CHECKS=passed
```

## Documentation corrections

Programmatic JSON/text/CSV checks confirmed:

```text
REQUIREMENTS_SOURCE_LABELS=LF,HF unchanged
REGISTER_SOURCE_LABELS=LF,HF values 1,2 unchanged
SOFTWARE_DESIGN_SOURCE_TEXT=LF,HF
EVENT_EXAMPLE_READER_SOURCE=LF
TEST_PLAN_SOURCE_EXPECTATION=uppercase
INVENTORY_SERIALIZED_SOURCE_EXPECTATION=uppercase
OTHER_ENUM_SERIALIZATION=lowercase
INVENTORY_ROWS=100 widths=13 statuses=designed
TST-RST-005=test-only white-box valid-state fixture
TST-STA-001=public observation plus test-only fixture
PRODUCTION_TRANSITION_OBSERVER_OR_STATE_FORCING_API=absent
```

DEC-059 records the uppercase-source clarification without changing numerical encodings.

## Test source status

The three required pytest files exist and compile syntactically. Static inspection confirms deterministic direct-value cases with no network, hardware, database, sleep, test ordering, shared mutable fixture, or later-module dependency.

A temporary inline standard-library harness supplied only the `raises` context and parameter-expansion mechanics used by these files, imported all three modules, and directly invoked every expanded case:

```text
SUPPLEMENTAL_DIRECT_TEST_CASES=114 passed
```

This was a supplemental implementation check, not pytest collection/execution, and no harness file was retained. Consequently, no pytest test is reported as passed and inventory status remains `designed`.

## Protected paths and scope

Final protected hashes match `audit/baselines/subproject_06_01_baseline.md`. Git path review finds no change to requirements, register model, architecture/mapping/diagrams, evidence, literature/source PDFs, bibliography, product material, plan, workflow handbook, implementation sequence, or prior audit records.

Production files are exactly `__init__.py`, `models.py`, `config.py`, and `clock.py`. Test files are exactly the three authorized unit files. No Wiegand, credential loader/repository, authorization, event log, output, watchdog, controller, CLI, script, experiment, physical interface, database, network, thread, async, lockfile, environment, or other production/test module exists.

## Git validation

Commands:

```text
git diff --check
git diff --name-only
git status --short --untracked-files=all
git rev-parse HEAD
```

Results:

- `git diff --check`: passed with no output.
- `HEAD`: `7a0b173724038aeac9f25f783a52a586a83619bf`.
- Exactly the 16 authorized paths below changed.
- No commit or push was performed.

```text
 M audit/file_change_ledger.md
 M docs/decision_log.md
 M docs/software_design.md
 M docs/test_case_inventory.csv
 M docs/test_plan.md
?? audit/baselines/subproject_06_01_baseline.md
?? audit/stage_reports/subproject_06_01.md
?? audit/validation/subproject_06_01_validation.md
?? pyproject.toml
?? src/elevator_access_sim/__init__.py
?? src/elevator_access_sim/clock.py
?? src/elevator_access_sim/config.py
?? src/elevator_access_sim/models.py
?? tests/unit/test_clock.py
?? tests/unit/test_config.py
?? tests/unit/test_models.py
```

## Unresolved concern and result

The only blocking concern is tool availability: pytest is not installed in any inspected local Python environment, and the task prohibits installing it. Compile, import, and independent standard-library validation pass, but the required pytest execution gate is unmet.

BLOCKED BEFORE NEXT STAGE — pytest is unavailable in the active Python environment
