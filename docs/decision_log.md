# Decision Log

| Decision ID | Date | Decision | Basis | Alternatives considered | Consequences | Status | Human approval required |
|---|---|---|---|---|---|---|---|
| DEC-001 | 2026-07-29 | Use STM32F10xxx documentation only as a representative reference architecture. | SRC-STM32-001 scope and UNK-004. | Identify the commercial processor; omit all representative embedded references. | Embedded concepts may be explained without asserting commercial hardware. | active scope rule | No for literature mapping; yes before selecting an implementation model. |
| DEC-002 | 2026-07-29 | Prohibit identification of the commercial processor without direct product evidence. | UNK-004 and SP-01 evidence limits. | Infer a processor from generic product category or reference source. | ARM, STM32, and STM32F103 claims remain prohibited. | active scope rule | No. |
| DEC-003 | 2026-07-29 | Treat ARMv7-A/R documentation as A/R-scope terminology only, not STM32 Cortex-M documentation. | SRC-ARM-002 preface. | Use it as generic proof for STM32 behavior. | Prevents profile conflation. | active scope rule | No. |
| DEC-004 | 2026-07-29 | Treat Wiegand-26 only as a possible future initial simulator format. | PRD-002; SRC-MISSING-004. | Assert product support; select another format without evidence. | No protocol implementation or requirement may be justified until a source is obtained. | deferred | Yes, with authoritative source. |
| DEC-005 | 2026-07-29 | Organize literature notes by engineering topic rather than filename. | SP-02 task requirement and SRC-WORKFLOW-001 bounded evidence practice. | Source-by-source summary. | Claim tracing is easier and large-manual scope stays narrow. | active | No. |
| DEC-006 | 2026-07-29 | Keep product evidence separate from external technical literature and reference design. | SP-01 classification, SRC-PLAN-001, SRC-WORKFLOW-001. | Merge sources into generic “facts.” | Prevents unsupported commercial-product claims. | active scope rule | No. |
| DEC-007 | 2026-07-29 | Register missing RFID, Wiegand, authorization, elevator, safety-terminology, and verification sources explicitly. | `evidence/unresolved_sources.md`. | Fill gaps with unsourced general knowledge. | Detailed technical requirements are gated pending authoritative sources. | active gate | Yes, to obtain/approve sources. |
| DEC-008 | 2026-07-29 | Model only an abstract 16-floor permission output. The software model will not specify physical elevator wiring, control motors, brakes, doors, or passenger-safety functions, and will make no certification or installation claim. Physical integration remains outside software-project scope. | SRC-PLAN-001 safety boundary, SP-02R completion instruction, and SP-03 project-owner authorization. | Block SP-03 on elevator standards; design physical integration without authority. | The elevator-source gap is non-blocking for requirements and simulation but remains blocking for every physical integration claim. | project-owner authorized; supervisor review pending | Yes — supervisor approval remains required before final submission or physical-scope expansion. |
| DEC-009 | 2026-07-29 | Accept four strong sources as the minimum literature completion set for SP-03 entry: SRC-RFID-001, SRC-WIEGAND-001, SRC-AUTH-001, and SRC-TEST-001. | SP-02R deadline-oriented sufficiency rule and claim traceability. | Continue broad research; proceed with unsupported claims. | Detailed standards coverage remains visible but non-blocking unless a later claim depends on it. | active stage decision | No; later design choices still require approval where recorded. |
| DEC-010 | 2026-07-29 | Freeze “Literature-Based Engineering Analysis and Software Simulation of a 16-Floor Dual-Frequency RFID Elevator Access-Control Controller” as the current working title. | SP-03 project-owner instruction. | Retain an unfrozen title; infer supervisor approval. | All SP-03 artifacts use one title while clearly recording pending supervisor approval. | project-owner approved working title | Yes — supervisor approval pending. |
| DEC-011 | 2026-07-29 | Freeze an abstract software-only access-authorization boundary. Exclude real reader electronics, RF physics, physical outputs and relays, mains wiring, elevator wiring, movement, motors, brakes, doors, passenger-safety logic, certification, and installation. | DEC-008; SRC-PLAN-001; SP-03 project-owner instruction. | Model physical integration; leave the boundary implicit. | Requirements and tests need no hardware and cannot support physical or safety claims. | active scope freeze | Yes — owner authorized; supervisor confirmation pending. |
| DEC-012 | 2026-07-29 | Use `LF` and `HF` only as logical simulated reader-source labels stored separately from credential bits. Both may deliver the same controller-side format. | CLM-021; SRC-RFID-001; SP-03 project-owner instruction. | Infer frequency from bits; model different RF protocols. | Dual-frequency scope is represented without RF or commercial-product claims. | proposed reference-design decision | Yes — confirm labels and interpretation. |
| DEC-013 | 2026-07-29 | Define `PROJECT_WIEGAND_26`: bit 1 even parity for bits 2–13, bits 2–9 facility code, bits 10–25 credential number, and bit 26 odd parity for bits 14–25. | SRC-WIEGAND-001 supports signaling/framing/parity generally; SP-03 owner instruction supplies the field allocation. | Leave fields undefined; claim a universal or product format. | The simulator has a testable parser contract; compatibility is not claimed. | proposed reference-design decision | Yes — confirm exact layout. |
| DEC-014 | 2026-07-29 | Number valid floors 1 through 16 and reject every other floor value. | SP-03 project-owner instruction; DEC-008. | Zero-based external floor numbering; variable floor count. | User-facing floor values have a fixed valid range. | proposed reference-design decision | Yes — confirm floor convention. |
| DEC-015 | 2026-07-29 | Represent floor permissions as an unsigned 16-bit mask: floor 1 maps to bit 0 and floor 16 to bit 15. | PRD-001; SP-03 project-owner instruction. | List/set representation; reverse bit order. | Authorization and exhaustive mapping tests have an unambiguous rule. | proposed reference-design decision | Yes — confirm mask convention. |
| DEC-016 | 2026-07-29 | Define the credential key as ordered composite `(facility_code, credential_number)` and reject duplicates without silent replacement. | SRC-AUTH-001 for authorization records; SP-03 owner instruction for fields; collision avoidance. | Arithmetic addition; silent deterministic replacement. | Facility 0–255 and credential 0–65535 remain distinct, collision-free fields. | proposed reference-design decision | Yes — confirm key and duplicate policy. |
| DEC-017 | 2026-07-29 | Permit at most one active abstract floor output at a time and activate only the granted floor channel. | SP-03 project-owner instruction; minimum-complexity rule. | Multiple concurrent outputs. | Output invariants are small and deterministic. | proposed reference-design decision | Yes — confirm one-output policy. |
| DEC-018 | 2026-07-29 | Reject every new request while an output is active as `controller_busy`; do not replace, extend, or otherwise alter the existing authorized activation. | SP-03 preference for the simplest testable policy. | Replace the activation; queue the request. | Failure paths create no new activation while the approved timeout remains deterministic. | proposed reference-design decision | Yes — confirm busy policy. |
| DEC-019 | 2026-07-29 | Use configurable logical output duration with default 3000 ms and valid range 100–30000 ms inclusive, driven by simulated monotonic time. | SP-03 project-owner instruction; SRC-TEST-001 repeatability principles. | Real-time waits; unbounded duration. | Timing is testable without waiting and invalid configuration is rejected. | proposed reference-design decision | Yes — confirm default and range. |
| DEC-020 | 2026-07-29 | Use a simulated watchdog with default timeout 2000 ms; missed service deactivates all outputs, logs watchdog reset, clears transient state, returns to idle, and permits later processing. | Representative watchdog concepts in SRC-STM32-001 and SRC-ARMADA-001; SP-03 owner instruction. | Omit watchdog; model commercial hardware behavior. | Fault recovery has a deterministic software-only contract. | proposed reference-design decision | Yes — confirm timeout and recovery behavior. |
| DEC-021 | 2026-07-29 | Use an event schema with sequence number, simulated timestamp, event type, source when applicable, facility code when available, credential number when available, requested floor when applicable, result, and reason. | SRC-AUTH-001 logging basis; SP-03 project-owner instruction. | Free-form text-only log; infer commercial schema. | Required decisions and recovery can be reconstructed and machine-read. | proposed reference-design decision | Yes — confirm schema. |
| DEC-022 | 2026-07-29 | Require scalability sizes 10, 100, 1,000, and 10,000 credentials and collect case counts, denials by reason, validation failures, average/median/p95 processing time, throughput, credential count, and seed/configuration ID. | SP-03 owner instruction; SRC-TEST-001 reproducibility and evaluation guidance. | Strict real-time target; ad hoc unrecorded sizes. | A bounded reproducible software-model experiment is defined. | active experiment requirement | Yes — confirm workload sizes and metrics. |
| DEC-023 | 2026-07-29 | Classify the requirements, context diagram, architecture/state model, Python simulator, simple CLI, automated tests, fault tests, reproducible experiments/data/plots, report, presentation, and reproduction instructions as required; classify GUI, physical prototype, real reader, server/network/cloud/mobile features, cryptographic emulation, physical elevator interface, certification, and exact board reproduction as optional or out of current scope. | SP-03 project-owner instruction and project deadline. | Treat all planned extensions as required. | Required and optional deliverables are visibly separated. | active scope decision | Yes — confirm deliverable split. |
| DEC-024 | 2026-07-29 | Freeze the SP-03 MVP behavior and requirements in `docs/requirements.md`; optional work shall not delay or become a prerequisite for the MVP. | SRC-PLAN-001 minimum-project rule; SP-03 project-owner instruction. | Continue adding behavior before architecture; leave decisions implicit. | SP-04 can design against a bounded specification without inventing major behavior. | scope frozen for human review | Yes — confirm the complete MVP. |
| DEC-025 | 2026-07-29 | Supersede only the obsolete blocking language in DEC-004 and DEC-007: SRC-WIEGAND-001 now supports general Wiegand signaling, SP-03 may define a proposed project Wiegand-26 profile, and RFID, authorization, and verification source blockers are resolved for software-model work. Product behavior and physical integration remain unresolved. | SP-02R source completion; DEC-009; current unresolved-source register. | Delete history; retain all original blocker wording as current. | Earlier decisions remain visible while their stale source-blocking effect no longer blocks SP-03. | active superseding decision | No; proposed design choices still require human review. |
| DEC-026 | 2026-08-03 | Accept the SP-03 frozen requirements and proposed defaults as the conceptual-design basis for SP-04. | SP-04 project-owner instruction and accepted commit `94b051c`. | Reopen requirements; wait for supervisor approval. | Architecture may proceed without changing requirements; supervisor approval remains pending. | project-owner approved stage basis | Yes — supervisor confirmation remains pending. |
| DEC-027 | 2026-08-03 | Treat power, reader electronics, input conditioning/isolation, output drivers/isolation, and physical elevator interface as conceptual-only blocks outside the Python simulator. | DEC-011 and SP-04 boundary. | Omit conceptual context; specify physical electronics. | Architectural context is complete without electrical claims. | active architecture boundary | Yes — confirm conceptual block boundary. |
| DEC-028 | 2026-08-03 | Separate frame, repository, authorization, output, coordinator, watchdog, logger, clock, configuration, CLI, and harness responsibilities with one owner per mutable state category. | SP-04 responsibility list and NFR-002. | One monolithic controller; shared mutable ownership. | SP-05 can define modular APIs without circular state ownership. | proposed architecture decision | Yes — confirm responsibility split. |
| DEC-029 | 2026-08-03 | Use states RESETTING, INITIALIZING, IDLE, VALIDATING, LOOKUP, AUTHORIZING, and OUTPUT_ACTIVE. | SP-04 compact-state instruction and frozen behavior. | Add parser substates or a generic error state. | Required transitions remain explicit without implementation-level detail. | proposed architecture decision | Yes — confirm state enumeration. |
| DEC-030 | 2026-08-03 | In IDLE process source, container/length, bits, parity, decode, lookup, enabled state, floor, mask, event precommit, activation, and expiry in the documented deterministic order. | SP-04 request-order instruction; frozen requirements. | Validate in implementation-dependent order. | Failure precedence and architectural ownership are testable. | proposed architecture decision | Yes — confirm processing order. |
| DEC-031 | 2026-08-03 | In OUTPUT_ACTIVE detect busy before inspecting new source, frame, or floor; preserve activation and expiry and attempt one busy event. | SP-04 explicit ambiguity resolution; DEC-018. | Validate before busy; queue or replace. | Busy outcomes are independent of new request contents. | proposed architecture decision | Yes — confirm busy-before-validation. |
| DEC-032 | 2026-08-03 | Startup clears all runtime/log state before loading configuration/repository; manual/watchdog reset clears outputs/transients but preserves valid configuration, credentials, event history, and sequence progression. | SP-04 reset instruction; RST requirements. | Erase credentials/events; preserve transient output state. | Reset is deterministic and audit history survives runtime recovery. | proposed architecture decision | Yes — confirm preservation rules. |
| DEC-033 | 2026-08-03 | Service the simulated watchdog at successful initialization and committed coordinator checkpoints; fault injection suppresses service without stopping logical time. | SP-04 watchdog instruction; DEC-020. | Background wall-clock thread; MCU-specific model. | Tests can deterministically trigger exactly one expiry request. | proposed architecture decision | Yes — confirm checkpoints. |
| DEC-034 | 2026-08-03 | Store every event field in every record and represent unavailable conditional values as explicit null. Sequence numbers begin at 1 on first successful append. | SP-04 event-schema ambiguity resolution; DEC-021. | Omit unavailable fields; use free-form records. | Event schemas are stable and machine-readable. | proposed architecture decision | Yes — confirm null policy. |
| DEC-035 | 2026-08-03 | Use the canonical state, source, event, result, and reason names and unique numerical register encodings defined in `docs/register_model.md`; textual names remain canonical. | SP-04 register/event instruction. | Text only; hardware-like encodings without qualification. | Logical observation is deterministic without implying processor behavior. | proposed architecture decision | Yes — confirm enumerations. |
| DEC-036 | 2026-08-03 | Organize state into nine named logical memory regions owned by configuration, coordinator, repository, output, watchdog, logger, and harness responsibilities. | SP-04 logical memory requirement. | Assign physical Flash/SRAM addresses or capacities. | Reset/persistence expectations are explicit and processor-neutral. | proposed architecture decision | Yes — confirm logical regions. |
| DEC-037 | 2026-08-03 | Use the compact 19-register, 32-bit-aligned logical map beginning at `0x0000` in `docs/register_model.md`. | SP-04 register requirements. | No register view; MCU peripheral map. | SP-05 has stable observation/control semantics for documentation and testing. | proposed architecture decision | Yes — confirm register model. |
| DEC-038 | 2026-08-03 | Invalid configuration, invalid credential records, duplicates, or repository initialization failure keep the controller in INITIALIZING, all outputs inactive, and normal processing disabled until corrected and reinitialized. | SP-04 fail-closed initialization rule. | Substitute defaults silently; enter IDLE with partial data. | Initialization cannot issue an access grant from invalid state. | proposed reliability policy | Yes — confirm initialization policy. |
| DEC-039 | 2026-08-03 | Gate grant activation on successful grant-event append; other logging failures expose logging_error without reversing required timeout/reset transitions or changing an existing output. | SP-04 event-log failure policy. | Grant despite missing log; roll back timeout/reset; add redundant logging. | Access decision ordering and failure effects are deterministic. | proposed reliability policy | Yes — confirm logging-failure policy. |
| DEC-040 | 2026-08-03 | Consider architecture ready for SP-05 design when all required requirements map, ownership/interfaces/states/registers/failures are consistent, diagrams exist, and no implementation behavior remains majorly undefined. | SP-04 completion standard. | Require industrial/electrical completeness; proceed with unmapped behavior. | Human review focuses on bounded conceptual completeness. | stage readiness criterion | Yes — review architecture coverage. |
| DEC-041 | 2026-08-03 | Use the `src/elevator_access_sim` package with models, config, clock, Wiegand, credentials, authorization, outputs, watchdog, event log, controller, and thin CLI modules; keep the later experiment runner outside the domain package. | SP-05 package plan and DEC-028 ownership. | Monolithic module; earlier generic package name; framework-heavy layers. | Module responsibilities and dependency direction are fixed before implementation. | proposed software-design decision | Yes — confirm package layout. |
| DEC-042 | 2026-08-03 | Use fixed-value enums and frozen immutable dataclasses for shared requests, credentials, decisions, events, configuration, responses, and snapshots; canonical lowercase text and SP-04 numerical encodings remain unchanged. | SP-05 shared-type requirement; DEC-034–DEC-035. | Dictionaries/free-form strings; mutable cross-module records. | Boundaries are typed and event/register semantics remain stable. | proposed software-design decision | Yes — confirm types and encodings. |
| DEC-043 | 2026-08-03 | Represent expected invalid, denied, granted, timeout, and reset outcomes with typed Result/Reason values; reserve the small exception hierarchy for startup data, infrastructure, invariants, and programmer misuse. | SP-05 domain-outcome policy. | One exception per denial; untyped sentinel values; catch all defects. | Normal access control flow is explicit and internal defects still fail tests. | proposed error-policy decision | Yes — confirm conversion boundary. |
| DEC-044 | 2026-08-03 | Use strict UTF-8 JSON configuration schema version 1 with exactly the five frozen fields; reject unknown, missing, wrong-type, out-of-range, duplicate-member, and unsupported-version data without silent defaults. | SP-05 configuration schema; TIM-001; TIM-003. | Loose mappings; YAML; JSON Schema dependency; permissive defaults. | Startup configuration is small, dependency-free, and typo-resistant. | proposed data-format decision | Yes — confirm strict schema. |
| DEC-045 | 2026-08-03 | Use strict UTF-8 JSON credential schema version 1 with validated ordered records, optional non-empty label, deterministic order, empty-repository support, and all-or-nothing duplicate rejection. | SP-05 credential schema; DAT-004–DAT-006. | CSV; database server; silent replacement; partial load. | The required repository can be reproduced and validated before publication. | proposed data-format decision | Yes — confirm record and label rules. |
| DEC-046 | 2026-08-03 | Use UTF-8 JSON Lines as preferred event export with all nine fields, explicit nulls, deterministic practical field order, and canonical lowercase enums; the in-memory list remains authoritative. | SP-05 serialization requirement; DEC-034. | CSV; omitted fields; persistent logger as source of truth. | One successful append maps to one stable machine-readable object. | proposed data-format decision | Yes — confirm export format. |
| DEC-047 | 2026-08-03 | Schedule an internal logical heartbeat every `max(1, watchdog_timeout_ms // 2)` while enabled; service occurs unless fault suppression is active, and reset reinitialization clears that transient suppression. This clarifies DEC-033 without rewriting its history. | SP-05 mandatory timing clarification; default 3000 ms output and 2000 ms watchdog; SP-04 transient-reset policy. | Coordinator checkpoints only; wall-clock thread; disable watchdog while active; preserve injected suppression across reset. | Normal long activations remain serviced while suppression deterministically exposes one expiry and later operation resumes normally. | proposed watchdog clarification | Yes — confirm formula and reset clearing. |
| DEC-048 | 2026-08-03 | At one simulated timestamp process normal unsuppressed heartbeat service, then watchdog expiry, then output expiry; watchdog reset cancels a coincident output timeout and produces exactly one watchdog outcome. | SP-05 scheduler contract. | Output first; unspecified ordering; process both reset and timeout. | Collisions, large/small advances, and idempotence have one expected result. | proposed scheduler decision | Yes — confirm priority. |
| DEC-049 | 2026-08-03 | Advance simulated monotonic time directly between due events with no background threads, asynchronous framework, real-time sleep, or physical watchdog. | TIM-002; NFR-001; SP-05 design principles. | Per-millisecond loops; timer threads; wall clock. | Automated timing tests are fast, deterministic, and hardware-free. | active implementation constraint | No within the approved software-only scope. |
| DEC-050 | 2026-08-03 | Parse and validate configuration plus credential candidates completely before atomically publishing normal controller startup state; never substitute a default for explicitly invalid data. | DEC-038; SP-05 atomicity rules. | Incremental publication; partial repository; fallback after errors. | Initialization remains all-inactive and non-operational on any startup defect. | proposed startup policy | Yes — confirm atomic loading boundary. |
| DEC-051 | 2026-08-03 | Use a validated in-memory credential repository as the required MVP, with persistence and database servers deferred/prohibited respectively. | DAT-005; NFR-010; deadline scope. | SQLite now; ORM/server; flat scan without key contract. | Lookup and experiments remain small and reproducible without infrastructure. | active MVP scope decision | No; later persistence needs review. |
| DEC-052 | 2026-08-03 | Expose initialize, submit, simulated-time advance, manual reset, watchdog suppression, immutable snapshot, and immutable event retrieval through the controller; other managers are component/testing contracts rather than alternate orchestration paths. | SP-05 public API boundary; ARC-CTL-001. | CLI-owned rules; public mutable managers; monolithic run function. | Busy priority, logging gate, scheduler, and reset policy have one coordinator. | proposed public-API decision | Yes — confirm signatures and no-op advance response. |
| DEC-053 | 2026-08-03 | Use pytest as the required test framework and one repository-root command; standard library plus pytest is sufficient and no real wait, network, hardware, database service, or GUI may be required. | NFR-003–NFR-005; SP-05 test plan. | Unittest-only custom runner; integration services; manual timing. | Verification remains reproducible and deadline-bounded. | active verification decision | No. |
| DEC-054 | 2026-08-03 | Require behavioral coverage of all required requirements, canonical denial/error reasons, seven states and transitions, 16 floor bits, public APIs, external validations, reset/watchdog policies, and LF/HF replay; source percentage is secondary. | VER-001–VER-004; SP-05 coverage expectation. | Percentage-only gate; sample happy paths. | Test completeness is measured against frozen behavior. | proposed verification decision | Yes — review inventory coverage. |
| DEC-055 | 2026-08-03 | For 10/100/1000/10000 credentials, generate at least `max(1000, credential_count)` mixed requests from a recorded seed, run one warm-up and three measured repetitions, and aggregate the frozen counts and host timing metrics with nearest-rank p95. | VER-005–VER-008; DEC-022. | Microbenchmark lookup only; strict threshold; one run. | Results are comparable, bounded observations of the Python model. | proposed experiment decision | Yes — confirm workload mix before execution. |
| DEC-056 | 2026-08-03 | Permanently retain small canonical fixtures/vectors, generator configuration/seed, aggregate results, and environment metadata; regenerate large credential/request sets. | NFR-006–NFR-008; repository hygiene. | Commit all generated inputs; retain only summaries without seed/config. | Reproducibility is preserved without repository bloat. | proposed preservation decision | Yes — confirm later result paths. |
| DEC-057 | 2026-08-03 | Divide SP-06 into the 11 bounded tasks in `docs/implementation_sequence.md`, adding tests with each behavior and postponing complete integration/experiments/document reconciliation to their named tasks. | Evidence-gated workflow and SP-05 instruction. | One large implementation prompt; code first and tests later. | Each change has a narrow scope, validation command, and completion gate. | active implementation-planning decision | Yes — accept sequence before SP-06. |
| DEC-058 | 2026-08-03 | Consider SP-05 ready for human review when contracts, APIs, types, schemas, timing priority, test inventory, verified vectors, fault paths, scale design, and bounded tasks are complete and no production code/tests exist. | SP-05 readiness criteria. | Require implementation/proof now; proceed with unresolved behavior. | Human review can approve a complete design without false implementation claims. | stage readiness criterion | Yes — review the SP-05 package. |
